apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample-ingress
  namespace: {{ .Values.namespace }}
  annotations:
{{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
{{- end }}
spec:
  rules:
  - http:
      paths:
      - path: {{ .Values.ingress.rules.api.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.ingress.rules.api.serviceName }}
            port:
              number: {{ .Values.ingress.rules.api.servicePort }}
      - path: {{ .Values.ingress.rules.ui.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.ingress.rules.ui.serviceName }}
            port:
              number: {{ .Values.ingress.rules.ui.servicePort }}
